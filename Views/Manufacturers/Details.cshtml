@model RemnantsProject.Data.Manufacturer

@{
    ViewData["Title"] = "Details";
}
<div id="movepagedown">
    <h4>Details about the manufacturer:</h4>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div id="slabEdit">
    <a asp-action="Edit" class="btn btn-primary" asp-route-id="@Model?.Id">Edit</a>
</div>
